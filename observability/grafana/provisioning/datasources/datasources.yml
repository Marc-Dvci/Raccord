apiVersion: 1

datasources:
  - name: Raccord Metrics
    uid: raccord-prom
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    jsonData:
      httpMethod: POST
      exemplarTraceIdDestinations:
        - name: trace_id
          datasourceUid: raccord-tempo

  - name: Raccord Logs
    uid: raccord-loki
    type: loki
    access: proxy
    url: http://loki:3100
    jsonData:
      derivedFields:
        - datasourceUid: raccord-tempo
          matcherRegex: 'trace_id=(\w+)'
          name: TraceID
          url: '$${__value.raw}'

  - name: Raccord Traces
    uid: raccord-tempo
    type: tempo
    access: proxy
    url: http://tempo:3200
    jsonData:
      tracesToLogsV2:
        datasourceUid: raccord-loki
        spanStartTimeShift: '-2m'
        spanEndTimeShift: '2m'
        filterByTraceID: false
        tags: [{ key: 'component', value: 'service' }]
      tracesToProfiles:
        datasourceUid: raccord-pyro
        profileTypeId: 'process_cpu:cpu:nanoseconds'
      serviceMap:
        datasourceUid: raccord-prom
      nodeGraph:
        enabled: true

  - name: Raccord Profiles
    uid: raccord-pyro
    type: grafana-pyroscope-datasource
    access: proxy
    url: http://pyroscope:4040
